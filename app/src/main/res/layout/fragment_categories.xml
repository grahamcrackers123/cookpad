<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CategoriesFragment">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/categoriesAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/orange">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/categoriesToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="Categories"
            app:titleTextColor="@android:color/white"
            app:menu="@menu/search_menu"
            android:theme="@style/ThemeOverlay.WhiteIconToolbar" />

    </com.google.android.material.appbar.AppBarLayout>

    <com.google.android.material.search.SearchBar
        android:id="@+id/categoriesSearchBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/margin_medium"
        android:hint="@string/search_categories"
        android:visibility="gone"
        app:layout_anchor="@id/categoriesAppBar"
        tools:visibility="visible"/>

    <com.google.android.material.search.SearchView
        android:id="@+id/categoriesSearchView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="@string/search_categories"
        app:layout_anchor="@id/categoriesSearchBar">

    </com.google.android.material.search.SearchView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/categoriesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:listitem="@layout/category_item"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
